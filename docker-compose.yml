services:
  train-catcher:
    build: .
    container_name: train-catcher
    ports:
      - "3300:3300"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./.env:/.env:ro
    restart: unless-stopped
