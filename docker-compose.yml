services:
  train-catcher:
    build: .
    container_name: train-catcher
    ports:
      - "3300:3300"
    environment:
      - FLASK_ENV=production
      - DB_PATH=/app/data/trains.db
    volumes:
      - ./.env:/app/.env:ro
      - ./data:/app/data
    restart: unless-stopped
